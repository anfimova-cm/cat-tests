package cat_tests.tests;

import org.testng.annotations.Test;

import java.io.BufferedReader;
import java.io.FileReader;
import java.io.FileWriter;
import java.io.IOException;

public class Notes {

    /*
    By.xpath("//a[contains(text(),'часть текста')]")
    By.xpath("//button[contains(@class,'btn-text') and contains(text(), 'Добавить')]")

    ---------------

    selectByVisibleText:
    Select selectByVisibleText = new Select (driver.findElement(By.id(“id_of_some_element”)));
    selectByVisibleText.selectByVisibleText(“some_visible_text”);
    */

}
